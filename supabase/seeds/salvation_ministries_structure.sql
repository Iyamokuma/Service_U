-- Salvation Ministries location catalog
-- Locations are created via admin Data Entry and published to directory_countries,
-- directory_states, directory_branches, churches, and satellite_church_sites.
-- This seed file intentionally contains no bulk location data.

-- Example (run manually after data entry approval, or use the admin Locations UI):
-- insert into public.directory_countries (id, name, branch_country_code) values (1, 'Nigeria', 'NG');
